name: "workflow example"
on: workflow_dispatch
jobs:
  first-job:
    runs-on: "ubuntu-latest"
    steps:
      - name: First step
        run: |
          echo "Urvesh"
          echo "Nimavat"
    


  
  
